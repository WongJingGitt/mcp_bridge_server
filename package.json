
{
  "name": "mcp-bridge-server",
  "version": "1.0.0",
  "description": "DeepSeek MCP 本地桥接服务",
  "main": "mcp-bridge-server.js",
  "scripts": {
    "start": "node mcp-bridge-server.js",
    "build": "pkg . --targets node18-win-x64,node18-macos-x64,node18-linux-x64 --output dist/mcp-bridge"
  },
  "pkg": {
    "assets": [
      "node_modules/@modelcontextprotocol/**/*"
    ],
    "outputPath": "dist"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  }
}